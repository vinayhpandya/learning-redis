global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: 'redis_exporter'
    scrape_interval: 15s
    static_configs:
      # 'redis_exporter' is the service name on the compose network.
      - targets: ['redis_exporter:9121']
      
